apiVersion: v1
kind: Service
metadata:
  name: codeshare-client-service
  labels:
    app: codeshare-client
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30080
  selector:
    app: codeshare-client
