apiVersion: v1
kind: Service
metadata:
  name: codeshare-server-service
  labels:
    app: codeshare-server
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: codeshare-server
