apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  labels:
    app: codeshare-fastapi
spec:
  type: ClusterIP
  selector:
    app: codeshare-fastapi
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
