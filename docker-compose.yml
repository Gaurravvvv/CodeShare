version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: codeshare-redis
    # Redis port NOT exposed to host for security — only accessible within Docker network
    volumes:
      - redis-data:/data
    command: redis-server --notify-keyspace-events Ex --requirepass ${REDIS_PASSWORD:-codeshare_redis_dev_password}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-codeshare_redis_dev_password}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - codeshare-net
    restart: unless-stopped
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: codeshare-server
    ports:
      - '3001:3001'
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    environment:
      - REDIS_URL=redis://:${REDIS_PASSWORD:-codeshare_redis_dev_password}@redis:6379
      - FASTAPI_URL=http://fastapi:8000
      - NODE_ENV=production
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/api/health', (r) => r.statusCode === 200 ? process.exit(0) : process.exit(1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - codeshare-net
    restart: unless-stopped
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'

  fastapi:
    build:
      context: ./fastapi-server
      dockerfile: Dockerfile
    container_name: codeshare-fastapi
    ports:
      - '8000:8000'
    env_file:
      - ./server/.env
    environment:
      - REDIS_URL=redis://:${REDIS_PASSWORD:-codeshare_redis_dev_password}@redis:6379
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - codeshare-net
    restart: unless-stopped
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: codeshare-client
    ports:
      - '80:80'
    depends_on:
      server:
        condition: service_healthy
    healthcheck:
      # Simplified: using wget which comes pre-installed in alpine
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s # Gives Nginx time to start before checking
    networks:
      - codeshare-net
    restart: unless-stopped
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'

networks:
  codeshare-net:
    driver: bridge

volumes:
  redis-data:
